css,img,jsはtwiiterbootstrapの機能拡張用
classはDB操作の一覧とメール送信
conf.phpで共通設定管理

index.phpよりログイン

管理画面はadmin,admin_edit 0302,kenken

test,index_htmlは無視。

メールの自動送信は若干のタイムラグがある。

-----------------

git rm f
git rm cached f


git push 